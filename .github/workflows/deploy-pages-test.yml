name: Test Deploy to GitHub Pages

on:
  pull_request:
    branches:
      - main
    paths:
      - 'docs/**'
      - '.github/workflows/deploy-pages.yml'
      - '.github/workflows/deploy-pages-test.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref == 'refs/heads/main' && github.run_id || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  test-deploy:
    name: Test Deploy to GitHub Pages
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: docs
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Detect generated integration pull request
        id: generated_integration
        uses: ./.github/actions/detect-generated-integration
      - uses: actions/setup-node@v7.0.0
        if: steps.generated_integration.outputs.is_generated_integration != 'true'
        with:
          node-version: 24
          cache: yarn
          cache-dependency-path: 'docs/yarn.lock'

      - name: Install dependencies
        if: steps.generated_integration.outputs.is_generated_integration != 'true'
        run: yarn install --frozen-lockfile
      - name: Test build website
        if: steps.generated_integration.outputs.is_generated_integration != 'true'
        run: yarn build
