# Repository guidance

ModularPipelines is a C# pipeline framework with dependency-driven, parallel modules.

## Build and test

- Use the SDK pinned in `global.json`.
- [Directory.Build.props](Directory.Build.props) enables preview C#, including collection-expression arguments such as `[with(StringComparer.Ordinal), ...]`. The pinned SDK compiles this syntax and `dotnet format` emits it. Verify syntax concerns with that toolchain rather than older language-version assumptions.
- Run every local `dotnet` command through `scripts/Invoke-AgentDotNet.ps1`, in-process as below. From another shell, use `pwsh -Command "& scripts/Invoke-AgentDotNet.ps1 ..."`; `pwsh scripts/Invoke-AgentDotNet.ps1 ...` loses array binding.
- The guard enforces `-TimeoutSeconds` (default 600) and a memory limit. Allow any outer execution timeout at least 30 seconds beyond `-TimeoutSeconds`. On exit `124` (timeout) or `137` (memory limit), report the validation limit and defer the expensive check to CI; do not automatically raise limits and retry.
- Build the affected solution: `ModularPipelines.slnx` for core, `ModularPipelines.Tests.slnf` for core plus tests, or `src/ModularPipelines.<Tool>/ModularPipelines.<Tool>.slnx` for one integration (including its tests when present). Analyzer and example solutions are available for those areas.
- Do not build or format `ModularPipelines.All.slnx`, or execute `src/ModularPipelines.Build`: these can exhaust workstation resources. A full build requires an explicit human request and sufficient capacity. Validate build-pipeline changes by building only its project.
- Run the relevant TUnit executable test project. For focused runs, append `--treenode-filter` and one pattern after `--`, such as `/*/*/MyTests/*`; combine patterns only with TUnit OR parentheses, such as `/*/*/(ClassA)|(ClassB)/*`.
- Keep integration tests in their matching `ModularPipelines.<Tool>.UnitTests` project; do not add integration references to the core unit tests.

```powershell
& scripts/Invoke-AgentDotNet.ps1 -SingleNode `
  -DotNetArguments @('build', 'ModularPipelines.slnx', '-c', 'Release')

& scripts/Invoke-AgentDotNet.ps1 `
  -DotNetArguments @('run', '--project', 'test/ModularPipelines.UnitTests/ModularPipelines.UnitTests.csproj', '--framework', 'net10.0', '--', '--coverage', '--coverage-output-format', 'cobertura')

# Format the affected solution before committing code changes.
& scripts/Invoke-AgentDotNet.ps1 `
  -DotNetArguments @('format', 'ModularPipelines.slnx')
```

- CI checks formatting with `format <solution> --verify-no-changes --severity info`.
- For documentation changes, use the Node version range in `docs/package.json` and run `yarn install` and `yarn build` from `docs/`.

## Code constraints

- Tool options are generated by `tools/ModularPipelines.OptionsGenerator/`. Fix the scraper or generator, preferably its generic logic, and regenerate; never hand-edit generated options, including during review or simplification.
- Current tool output is authoritative, even when regeneration breaks an earlier API. Do not preserve generated APIs with handwritten shims, aliases, obsolete forwarding members, or manual extension files.
- Generated CLI integration packages do not maintain `PublicAPI.Shipped.txt` or `PublicAPI.Unshipped.txt`. Do not create, synchronize, stage, or repair these baselines in generated-options PRs; validate generated output through builds, tests, and command coverage instead. Core and handwritten-package API tracking remains in place.
- Library code under `src/` uses `ConfigureAwait(false)`; tests need not.

## Pull request maintenance

- Merge an autogenerated CLI options PR only when all required checks pass, the PR is conflict-free, and review confirms healthy generated output with no outstanding findings. Pending checks or reviews are not failures; wait for their results before deciding.
- Close a conflicting or defective autogenerated CLI options PR; never rebase it, resolve its conflicts, or patch it in place. Regenerate from the latest `main` using the [Generate CLI Options workflow](.github/workflows/generate-cli-options.yml), confirm the fresh replacement PR, and link it to the closed PR; if regeneration produces no changes, record that result.
- For a defect (failing checks or correctness/review issues), before regenerating, raise or reuse linked issues against the responsible scraper or generator in `tools/ModularPipelines.OptionsGenerator/` with the affected CLI, failure evidence, and expected output, and fix the generator separately.
- Whenever any PR is closed, cancel its queued, waiting, and running CI jobs/workflow runs, including runs for superseded commits, and verify that no pending work remains. Scope cancellation to the closed PR; preserve unrelated runs and replacement generation.
- For any PR, after pushing review fixes and confirming them in the remote PR head, reply to each addressed finding with the fix commit and resolve its thread, whether a human or a bot opened it. Reply to review-body findings and request re-review when a changes-requested review is outstanding. Leave unaddressed findings and newer rebuttals open; do not dismiss reviews to bypass approval requirements.

Keep agent guidance focused on non-obvious repository constraints. Link to existing
configuration or scripts instead of duplicating their contents.
