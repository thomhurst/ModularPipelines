// <auto-generated>
// This file was generated by ModularPipelines.OptionsGenerator.
// Source: https://cloud.google.com/sdk/gcloud/reference/sql/instances/clone
// Do not edit this file manually.
// </auto-generated>

#nullable enable

using System.CodeDom.Compiler;
using System.Diagnostics.CodeAnalysis;
using ModularPipelines.Attributes;
using ModularPipelines.Google.Options;
using System.ComponentModel.DataAnnotations;

namespace ModularPipelines.Google.Options;

/// <summary>
/// clones a Cloud SQL instance
/// </summary>
[GeneratedCode("ModularPipelines.OptionsGenerator", "2.0.0")]
[ExcludeFromCodeCoverage]
[CliSubCommand("sql", "instances", "clone")]
public record GcloudSqlInstancesCloneOptions : GcloudOptions, IValidatableObject
{
    /// <summary>
    /// clones a Cloud SQL instance
    /// </summary>
    /// <param name="Source">Cloud SQL instance ID of the source.</param>
    /// <param name="Destination">Cloud SQL instance ID of the clone.</param>
    public GcloudSqlInstancesCloneOptions(
        string Source,
        string Destination
    )
    {
        global::System.ArgumentNullException.ThrowIfNull(Source);
        this.Source = Source;
        global::System.ArgumentNullException.ThrowIfNull(Destination);
        this.Destination = Destination;
    }

    public void Deconstruct(out string Source, out string Destination)
    {
        Source = this.Source;
        Destination = this.Destination;
    }

    /// <summary>
    /// Return immediately, without waiting for the operation in progress to complete.
    /// </summary>
    [CliFlag("--async")]
    public bool? Async { get; set; }

    /// <summary>
    /// The preferred secondary zone for the cloned regional instance. If you specify a value for this flag, then the destination instance uses the value as the secondary zone. The secondary zone can't be the same as the primary zone.
    /// </summary>
    [CliOption("--preferred-secondary-zone", Format = OptionFormat.EqualsSeparated)]
    public string? PreferredSecondaryZone { get; set; }

    /// <summary>
    /// The preferred zone for the cloned instance. If you specify a value for this flag, then the destination instance uses the value as the primary zone.
    /// </summary>
    [CliOption("--preferred-zone", Format = OptionFormat.EqualsSeparated)]
    public string? PreferredZone { get; set; }

    /// <summary>
    /// The time the source instance was deleted. This is required if cloning from a deleted instance.
    /// </summary>
    [CliOption("--source-instance-deletion-time", Format = OptionFormat.EqualsSeparated)]
    public string? SourceInstanceDeletionTime { get; set; }

    /// <summary>
    /// At most one of these can be specified: Binary log coordinates for point-in-time recovery. The name of the binary log file. Enable point-in-time recovery on the source instance to create a binary log file. If specified with &lt;--bin-log-position&gt; to form a valid binary log coordinate, it defines an earlier point in time to clone a source instance from. For example, mysql-bin.000001. This flag argument must be specified if any of the other arguments in this group are specified.
    /// </summary>
    [CliOption("--bin-log-file-name", Format = OptionFormat.EqualsSeparated)]
    public string? BinLogFileName { get; set; }

    /// <summary>
    /// At most one of these can be specified: Binary log coordinates for point-in-time recovery. Represents the state of an instance at any given point in time inside a binary log file. If specified along with &lt;--bin-log-file-name&gt; to form a valid binary log coordinate, it defines an earlier point in time to clone a source instance from. For example, 123 (a numeric value). This flag argument must be specified if any of the other arguments in this group are specified.
    /// </summary>
    [CliOption("--bin-log-position", Format = OptionFormat.EqualsSeparated)]
    public string? BinLogPosition { get; set; }

    /// <summary>
    /// At most one of these can be specified: Binary log coordinates for point-in-time recovery. Represents the state of an instance at any given point in time inside a transaction log file. For MySQL, the binary log file is used for transaction logs. For PostgreSQL, the write-ahead log file is used for transaction logs. For SQL Server, the log backup file is used for such purpose. To create a transaction log, enable point-in-time recovery on the source instance. Instance should have transaction logs accumulated up to the point in time they want to restore up to. Uses RFC 3339 format in UTC timezone. If specified, defines a past state of the instance to clone. For example, '2012-11-15T16:19:00.094Z'. This flag argument must be specified if any of the other arguments in this group are specified.
    /// </summary>
    [CliOption("--point-in-time", Format = OptionFormat.EqualsSeparated)]
    public string? PointInTime { get; set; }

    /// <summary>
    /// At most one of these can be specified: Binary log coordinates for point-in-time recovery. The name of the database to be restored for a point-in-time restore. If set, the destination instance will only restore the specified database.
    /// </summary>
    [CliOption("--restore-database-name", Format = OptionFormat.EqualsSeparated)]
    public string? RestoreDatabaseName { get; set; }

    /// <summary>
    /// Cloud SQL instance ID of the source.
    /// </summary>
    [CliArgument(0, Phase = CommandLinePhase.EarlyOperand, Required = true)]
    public string Source { get; private init; }

    /// <summary>
    /// Cloud SQL instance ID of the clone.
    /// </summary>
    [CliArgument(1, Phase = CommandLinePhase.EarlyOperand, Required = true)]
    public string Destination { get; private init; }

    /// <inheritdoc />
    IEnumerable<ValidationResult> IValidatableObject.Validate(ValidationContext validationContext)
    {
        if (((!string.IsNullOrWhiteSpace(BinLogFileName) || !string.IsNullOrWhiteSpace(BinLogPosition)) ? 1 : 0) + ((!string.IsNullOrWhiteSpace(PointInTime) || !string.IsNullOrWhiteSpace(RestoreDatabaseName)) ? 1 : 0) > 1)
        {
            yield return new ValidationResult("At most one of (BinLogFileName or BinLogPosition) or (PointInTime or RestoreDatabaseName) may be specified.", [nameof(BinLogFileName), nameof(BinLogPosition), nameof(PointInTime), nameof(RestoreDatabaseName)]);
        }
        if ((!string.IsNullOrWhiteSpace(BinLogFileName) || !string.IsNullOrWhiteSpace(BinLogPosition) || !string.IsNullOrWhiteSpace(PointInTime) || !string.IsNullOrWhiteSpace(RestoreDatabaseName)) && (!string.IsNullOrWhiteSpace(BinLogFileName) || !string.IsNullOrWhiteSpace(BinLogPosition)) && (!(!string.IsNullOrWhiteSpace(BinLogFileName))))
        {
            yield return new ValidationResult("BinLogFileName must be specified when other arguments in this group are specified.", [nameof(BinLogFileName)]);
        }
        if ((!string.IsNullOrWhiteSpace(BinLogFileName) || !string.IsNullOrWhiteSpace(BinLogPosition) || !string.IsNullOrWhiteSpace(PointInTime) || !string.IsNullOrWhiteSpace(RestoreDatabaseName)) && (!string.IsNullOrWhiteSpace(BinLogFileName) || !string.IsNullOrWhiteSpace(BinLogPosition)) && (!(!string.IsNullOrWhiteSpace(BinLogPosition))))
        {
            yield return new ValidationResult("BinLogPosition must be specified when other arguments in this group are specified.", [nameof(BinLogPosition)]);
        }
        if ((!string.IsNullOrWhiteSpace(BinLogFileName) || !string.IsNullOrWhiteSpace(BinLogPosition) || !string.IsNullOrWhiteSpace(PointInTime) || !string.IsNullOrWhiteSpace(RestoreDatabaseName)) && (!string.IsNullOrWhiteSpace(PointInTime) || !string.IsNullOrWhiteSpace(RestoreDatabaseName)) && (!(!string.IsNullOrWhiteSpace(PointInTime))))
        {
            yield return new ValidationResult("PointInTime must be specified when other arguments in this group are specified.", [nameof(PointInTime)]);
        }
        yield break;
    }

}
